export enum LogMessages {
  // Databases
  DATABASE_CONNECTED = "Database connected",
  DATABASE_CONNECTION_ERROR = "Error connecting to database",
  DATABASE_CONNECTION_CLOSED = "Database connection closed",
  DATABASE_CLOSE_ERROR = "Error closing database connection",
  // ... other messages
}
